# global (for shared code that uses DB_USER / DB_PASSWORD)
DB_USER=root
DB_PASSWORD=mersad
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=wisper

# compatibility duplicates (some services use DB_PASS / DB_PASSWORD)
DB_PASS=mersad

# Redis / common
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_ADDR=127.0.0.1:6379
REDIS_PASSWORD=
